import { Button } from '@/components/ui/button'
import styles from './page.module.scss'

const Home = () => {
  return (
    <div className={styles['container']}>
      <h1>Платформа независимых театров</h1>
      <Button> Test </Button>
    </div>
  )
}

export default Home
